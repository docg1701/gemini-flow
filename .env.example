# ==================================================
# Variáveis de Ambiente Globais para o Projeto Gemini-Flow
# ==================================================

# ------------------
# Backend (Python)
# ------------------
# Chave da API do Google Gemini (OBRIGATÓRIO)
# Obtenha sua chave em: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="SUA_CHAVE_API_AQUI"

# Nível de Log para o Backend (Opcional, Padrão: INFO via código)
# Valores possíveis: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Para usar, o código em backend/config.py ou backend/main.py precisa ser ajustado.
# PYTHON_LOG_LEVEL=INFO

# Modo de Debug para o Backend (Opcional, Padrão: False via código)
# Para usar, o código em backend/config.py precisa ser ajustado.
# PYTHON_DEBUG_MODE=False

# Usuário e Grupo para a aplicação Backend dentro do Docker (Padrão: appuser/appgroup)
BACKEND_APP_USER=appuser
BACKEND_APP_GROUP=appgroup

# Porta do container do Backend (Padrão: 8000) - Usada internamente no Docker e para mapeamento.
BACKEND_CONTAINER_PORT=8000
# Porta do Host para o Backend (Padrão: 8000) - Porta exposta na sua máquina.
BACKEND_HOST_PORT=8000


# ------------------
# Frontend (React & Nginx)
# ------------------
# URL Base da API do Backend para o Frontend (Opcional)
# Se não definida, o frontend tentará usar caminhos relativos.
# Para desenvolvimento local: http://localhost:${BACKEND_HOST_PORT} (ou o valor que você definir para BACKEND_HOST_PORT)
# O frontend (React) precisa ser configurado para usar esta variável (ex: process.env.REACT_APP_API_BASE_URL).
# REACT_APP_API_BASE_URL=http://localhost:8000

# Porta do container Nginx (Padrão: 80) - Usada internamente no Docker.
NGINX_CONTAINER_PORT=80
# Porta do Host para o Frontend/Nginx (Padrão: 3000) - Porta exposta na sua máquina.
FRONTEND_HOST_PORT=3000


# ------------------------------------
# Configurações de Build Docker
# ------------------------------------
# Versões das imagens base para os estágios do Dockerfile
DOCKER_NODE_VERSION=20-slim
DOCKER_PYTHON_VERSION=3.10-slim
DOCKER_NGINX_VERSION=1.25-alpine

# Opções de instalação do Poetry (avançado, geralmente não precisa mudar)
# POETRY_INSTALL_OPTIONS=--no-interaction --no-ansi --no-dev --no-root
